namespace AutoGenerated.Transfer
{
    using System;
    using NF.Network.Protocol.Interface;
    using AutoGenerated.Message;
    using AutoGenerated.Interface;

    public partial class Sender : NF.Network.Transfer.Protobuf.BaseSender,  I{{ name }}
    {
        #region I{{ name }} implementation
        public Task<R{{ name }}> {{ name }}(Q{{ name }} q)
        {
            return MessageSend<R{{ name }}> (q);
        }

        public Task<R{{ name }}> {{ name }}({{ q.arguments | join: ', ' }})
        {
            return {{ name }} (new Q{{ name }}() { {{ q.arguments | map: 'DuplicateAssignName' | join: ', ' }} });
        }
        #endregion
   }
}
