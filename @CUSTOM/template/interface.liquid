namespace AutoGenerated.Interface
{
    using System;
    using NF.Network.Protocol.Interface;
    using AutoGenerated.Message;

    public interface I{{ name }}: IInterfacedActor
    {
        Task<R{{ name }}> {{ name }}(Q{{ name }} q);
        Task<R{{ name }}> {{ name }}({{ q.arguments | join: ', ' }});
    }
}
